import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'org.leinelab.keyapp_capacitor',
  appName: 'leinelapp-capacitor',
  webDir: 'undefined'
};

export default config;
